# v1.0.1 release recovery execution plan

## 0. 文書管理

| 項目 | 値 |
|---|---|
| 対象repository | `dahatake/StudyReport-Evaluator` |
| 基準時刻 | 2026-09-03T09:02:12.3364026+09:00 |
| superseded plan | [`20260903-0605-TaskExecutionPlan.md`](20260903-0605-TaskExecutionPlan.md) のv1.0.0公開部分 |
| candidate version / tag | `1.0.1` / `v1.0.1` |
| 状態 | `SUPERSEDED_BY_1_1_0_DELIVERY` |
| 捏造防止 | 未観測値は未確定とし、command/APIの実測前に完了扱いしない |

> 2026-09-03の後続要求でWindows/macOS setup deliveryが追加され、版管理規則上MINORとなるため、未公開`v1.0.1`のrelease作業は実行しない。失敗した`v1.0.0`の証跡とP-01/P-02の修正は履歴・実装として保持し、`1.1.0` candidateへcarry forwardする。`v1.0.1` tagまたはReleaseは作成しない。

## 1. 発生事象と一次証跡

1. initial CI run [`33682636186`](https://github.com/dahatake/StudyReport-Evaluator/actions/runs/33682636186) はrelease commit `b68e7577df616c0e259b55697a52d26412a01208`に対して`completed/success`、required stepは全件成功した。
2. 2026-09-03T08:37:33+09:00の`GET /repos/dahatake/StudyReport-Evaluator/immutable-releases`は`enabled=true`、`enforced_by_owner=false`を返した。同時点でGitHub Releaseは0件だった。
3. release workflowはtag refを使って1回だけdispatchした。run [`33696306818`](https://github.com/dahatake/StudyReport-Evaluator/actions/runs/33696306818) は`head_branch=v1.0.0`、`head_sha=b68e7577df616c0e259b55697a52d26412a01208`、`run_attempt=1`だった。
4. run `33696306818`は`Verify release identity and privacy boundary`で失敗した。`dev/version.ps1`がrunner上の3つのGit application pathを1つのcommand名として扱ったことが直接原因である。
5. `Create GitHub Release`はskipped、workflow artifactは0件、`GET /releases/tags/v1.0.0`は404だった。公開物やdraft Releaseは作られていない。
6. remote annotated tag `v1.0.0`は引き続き`b68e7577df616c0e259b55697a52d26412a01208`を指す。このtagを削除、移動、再作成しない。

## 2. Recovery decision

[`dev/docs/version-management.md`](../dev/docs/version-management.md) §8.3に従い、共有済み`v1.0.0`を修正または再利用せず、新しいPATCH版`v1.0.1`へ移行する。同じrunのrerun、`v1.0.0`への再dispatch、main-only workaroundによる`v1.0.0`公開は禁止する。

`v1.0.1`は初回の公開GitHub Releaseとし、`CHANGELOG.md`の`1.0.1` sectionから、`1.0.0` candidateの利用者向け変更を含むこととrelease tooling修正を明示する。

## 3. Recovery tasks

| ID | Task | Exit gate | 状態 |
|---|---|---|---|
| P-01 | 複数application pathの解決を修正し回帰testを追加 | 複数`git.cmd`環境のtag/clean verifyを含むself-test 14 assertions成功 | DONE |
| P-02 | version、CHANGELOG、lock、README URL、文書契約を`1.0.1`へ同期 | `version.ps1 verify`、locked restore、documentation test成功 | IN_PROGRESS |
| P-03 | local required gateをcandidate sourceで再実行 | Release build、全required test、canonical sample identity/no-network E2E、package test成功 | NOT_RUN |
| P-04 | P-01〜P-03を敵対的レビューし指摘を反映 | blocker/high finding 0、再検証成功 | NOT_RUN |
| P-05 | release commitをmainへpushしCIを確認 | origin/main exact SHA、CI `completed/success` | NOT_RUN |
| P-06 | annotated `v1.0.1`を作成・検証・push | tag type=`tag`、target=release commit、clean verify成功 | NOT_RUN |
| P-07 | 公開前identity/idempotency/immutability gate後に1回だけdispatch | Release不存在、既存`v1.0.1` runなし、`enabled=true`、一意run ID | NOT_RUN |
| P-08 | release workflowと公開metadataを検証 | 全step成功、stable/public/immutable、asset 2件 | NOT_RUN |
| P-09 | public assetを認証なしでfresh downloadしdeep verify | sidecar/attestation/layout/version/CLI/cold launch/cleanup/link全件成功 | NOT_RUN |
| P-10 | closure文書を実測値で更新しcommit/push | C-026=`VERIFIED`、status=`RELEASED — v1.0.1`、値一致 | NOT_RUN |
| P-11 | closure commitの最終CIとremote整合を確認 | final CI成功、main/tag/Release/docs/worktree整合 | NOT_RUN |

## 4. Guardrails

- `v1.0.0` tag、failed run、logを変更しない。
- Releaseを検出した場合は重複dispatchしない。
- `v1.0.1`のRelease作成前にrepository immutabilityを再取得する。
- `/sample/`、workbook本文、private path、credential、full raw logをcommit/uploadしない。
- canonical sampleはexact pathだけを使用し、同directoryを列挙して代用しない。
- optional live Copilot／Excel recalculationをrequired gateへ合算しない。
- 各task後に独立した敵対的reviewを行い、実在するfindingだけを反映して再検証する。
- failure時は次phaseへ進まず、Release/tag/assetを差し替えない。

## 5. 未確定の実測値

次は実測前に値を記載しない。

- v1.0.1 release commit SHA
- v1.0.1 tag object SHA
- candidate CI run ID / conclusion
- release workflow run ID / conclusion
- GitHub Release ID / published-at / URL
- public ZIP bytes / SHA-256
- public bundled CLI SHA-256
- closure commit SHA
- final CI run ID / conclusion

全taskと敵対的reviewが実測証跡へ追跡できるまで、`v1.0.1` releaseを`COMPLETE`と表記しない。
